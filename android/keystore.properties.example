# Android Release Keystore Signing Configuration Template
# Copy this file to 'keystore.properties' (which is git-ignored) and fill in your actual values:
#
# storeFile=../my-release-key.jks
# storePassword=your-store-password
# keyAlias=your-key-alias
# keyPassword=your-key-password
